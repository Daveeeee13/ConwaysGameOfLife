Creare una applicazione mediante HTML/javasript/CSS che simuli il gioco Conway's Game of Life:

A. creare inizialmente una versione del gioco che utilizza una tabella quadrata di dimensione prefissata: 30x30 celle;
B. creare un form contenente un campo di input (textbox) tramite il quale l’utente inserisce il numero di righe della matrice (20 <= n <= 100);
C. applicare ad ogni cella le regole per la nascita/morte della cella.
